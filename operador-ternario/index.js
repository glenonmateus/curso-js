// ? :
// (condição) ? 'valor para verdadeiro' : 'valor para falso'
const pontuacao = 999;

// if (pontuacao > 999) {
//   console.log("Usuário VIP");
// } else {
//   console.log("Usuário normal");
// }

const nivel = pontuacao > 999 ? "Usuário VIP" : "Usuário normal";

const cor = null;
const corPadrao = cor || "Preto";
